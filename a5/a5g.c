// (a5f.c) Measure the speed with which one can write 8192 bytes 
// to the page disk cache. 
#include <stdio.h> 
int main() { 
    double seconds=0.0; 
    printf("%e seconds to write 8192 bytes to the processor memory cache.\n",seconds); 
    return 0;
} 
