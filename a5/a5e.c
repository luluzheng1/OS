// (a5e.c) Measure the speed with which one can write to the local directory 
// /tmp on your machine. 
#include <stdio.h> 
int main() { 
    double seconds=0.0; 
    printf("%e seconds to write 8192 bytes to /tmp\n", seconds); 
    return 0;
} 
