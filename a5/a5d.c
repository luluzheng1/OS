// (a5d.c) Measure the speed with which one can write to the disk containing 
// your home directory (and/or class directory). 
#include <stdio.h> 
int main() { 
    double seconds=0.0; 
    printf("%e seconds to write 8192 bytes to network disk\n",seconds); 
    return 0;
} 
