// (a5c.c) Measure the speed with which one can read from the disk page cache. 
#include <stdio.h> 
int main() { 
    double seconds=0.0; 
    printf("%e seconds to read from 8192 bytes from disk page cache\n",seconds);; 
    return 0;
} 
